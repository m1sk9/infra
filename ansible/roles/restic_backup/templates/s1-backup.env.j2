# {{ ansible_managed }}
# restic environment for the s1-backup repository. Sourced by the backup
# script and the systemd service. Secrets originate from the Ansible vault.
RESTIC_REPOSITORY={{ restic_repository }}
RESTIC_PASSWORD={{ restic_password }}
AWS_ACCESS_KEY_ID={{ r2_access_key_id }}
AWS_SECRET_ACCESS_KEY={{ r2_secret_access_key }}
