# {{ ansible_managed }}
# Better Stack heartbeat endpoint for {{ item.name }}. The URL is the only
# credential involved — anyone holding it can report this service as healthy — so
# it comes from the vault and this file is root-only.
HEARTBEAT_URL={{ item.url }}
